import UIKit

//let test = "(https://golos.io/ru--kriptovalyuty/[@ivelon-ii](@ivelon-ii)/proekt-mile-global)"
//
//let pattern = "\\[\\@(\\w*)\\]\\("
//let mdImageRegex = try! NSRegularExpression(pattern: pattern, options: [])
//
//if let userNameMatches = mdImageRegex.firstMatch(in: test, options: .withTransparentBounds, range: NSRange(location: 0, length: test.count)) {
//    let fff = (test as NSString).substring(with: NSRange(location:  userNameMatches.range.location + 2, length:  userNameMatches.range.length - 4))
//
//    print(fff)
//}
